name: CI
on:
  push:
    branches: [main]
  pull_request:
permissions:
  contents: read
jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        # 3.11 is the declared floor (tomllib); 3.14 is the current CPython.
        python-version: ["3.11", "3.14"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - run: pip install .
      # Exact pins: floating test/lint tools on the CI path let a bad pytest
      # release red tag day silently. Bump deliberately, together with uv.lock.
      - run: pip install ruff==0.15.22 pytest==9.1.1
      - run: ruff check .
      - run: ruff format --check .
      - run: PYTHONPATH=src python -m pytest tests
